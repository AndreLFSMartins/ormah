# 03 — PR #133 lands, then PR #95 rebases

Type: task

## Question

Ruled review order: [PR #133](https://github.com/r-spade/ormah/pull/133) (delta-select watermark,
→ #81, `MERGEABLE`) is reviewed first; [PR #95](https://github.com/r-spade/ormah/pull/95) (batch K
pairs per LLM call, → #87) must **not** be rebased until #133 lands — avoids a double rebase
(#95 was conflicting, `mergeable=UNKNOWN`).

Task: shepherd #133 through review (ping @r-spade as needed); once it lands, rebase #95 on the
fresh `upstream/main` (fork workflow: push to `fork`) and record both events here. These two PRs
are the #81/#87 gate that blocks #224's macro planning (ticket 15).
