# 01 — PR #229 closed as superseded, confirmed

Type: task

## Question

[PR #229](https://github.com/r-spade/ormah/pull/229) (draft, @r-spade, branch
`feat/issue-220-223-memory-lifecycle`) implements all four issues in one PR and says
`Closes #220–#223`. Discord ruled it superseded by André's four separate PRs, but it is **still
open**. A stale `Closes #220` in an open PR will auto-close the assigned issues if it ever merges.

Task: ping @r-spade (or comment on the PR) to get it closed **before** the first of André's PRs
goes up; record the closure here. If @r-spade prefers to keep it open as reference, record that
ruling and the agreed guard against the auto-close hazard instead.
