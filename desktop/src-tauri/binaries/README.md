# binaries/

The frozen `ormah-server` sidecar lands here at build time as
`ormah-server-<target-triple>` (e.g. `ormah-server-aarch64-apple-darwin`),
produced by `desktop/scripts/build-sidecar.sh`. The binaries themselves are
git-ignored; this README keeps the directory present.
